// Los usuarios se crean a través de Supabase Auth (signUp action).
// El seed no aplica en este proyecto — la autenticación es gestionada por Supabase.
console.log("No hay seed configurado. Usa /register para crear tu primer usuario.")
